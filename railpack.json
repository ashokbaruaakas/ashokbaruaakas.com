{
    "$schema": "https://schema.railpack.com",
    "provider": "php",
    "steps": {
        "build": {
            "commands": [
                "...",
                "npm run build:ssr"
            ]
        }
    },
    "deploy": {
        "startCommand": "php artisan inertia:start-ssr & frankenphp run --config /etc/caddy/Caddyfile"
    }
}