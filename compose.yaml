services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
            target: app_runtime
        image: app-build:latest
        container_name: app-build
        restart: unless-stopped
        environment:
            - APP_ENV=${APP_ENV:-production}
            - APP_DEBUG=${APP_DEBUG:-false}
            - APP_URL=${APP_URL:-http://localhost}
            - DB_CONNECTION=mysql
            - DB_HOST=${DB_HOST}
            - DB_PORT=${DB_PORT:-3306}
            - DB_DATABASE=${DB_DATABASE}
            - DB_USERNAME=${DB_USERNAME}
            - DB_PASSWORD=${DB_PASSWORD}
            - REDIS_HOST=${REDIS_HOST}
            - REDIS_PASSWORD=${REDIS_PASSWORD}
            - REDIS_PORT=${REDIS_PORT:-6379}
            - SESSION_DRIVER=${SESSION_DRIVER:-database}
            - CACHE_DRIVER=${CACHE_DRIVER:-redis}
            - QUEUE_CONNECTION=${QUEUE_CONNECTION:-redis}
            - SSR_ENABLED=${SSR_ENABLED:-true}
            - SSR_SERVER=${SSR_SERVER:-http://ssr:13714}

    web:
        build:
            context: .
            dockerfile: Dockerfile
            target: web_runtime
        image: web-build:latest
        container_name: web-build
        restart: unless-stopped
        environment:
            APP_UPSTREAM: app

    ssr:
        build:
            context: .
            dockerfile: Dockerfile
            target: ssr_runtime
        image: ssr-build:latest
        container_name: ssr-build
        restart: unless-stopped
        environment:
            - NODE_ENV=production
            - APP_ENV=${APP_ENV:-production}
            - APP_DEBUG=${APP_DEBUG:-false}
            - APP_URL=${APP_URL:-http://localhost}
            - DB_CONNECTION=mysql
            - DB_HOST=${DB_HOST}
            - DB_PORT=${DB_PORT:-3306}
            - DB_DATABASE=${DB_DATABASE}
            - DB_USERNAME=${DB_USERNAME}
            - DB_PASSWORD=${DB_PASSWORD}
            - REDIS_HOST=${REDIS_HOST}
            - REDIS_PASSWORD=${REDIS_PASSWORD}
            - REDIS_PORT=${REDIS_PORT:-6379}
            - SESSION_DRIVER=${SESSION_DRIVER:-database}
            - CACHE_DRIVER=${CACHE_DRIVER:-redis}
            - QUEUE_CONNECTION=${QUEUE_CONNECTION:-redis}
